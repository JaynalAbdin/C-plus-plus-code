#include <bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    cin >> s;
    cout << s << endl;
    string rv = s;
    reverse(rv.begin(), rv.end());
    count << rv << endl;

    return 0;
}